{{- $version := "0.1" }}
{{- $image_version := $version | replace "." "-" }}
---
image: {{ .ModuleName }}/{{ .ImageName }}-src-artifact-{{ $image_version }}
final: false
from: {{ index $.Images "builder/distroless" }}
git:
- add: /{{ .ModulePath }}/modules/{{ .ModulePriority }}-{{ .ModuleName }}/images/{{ .ImageName }}/scripts
  to: /src/scripts
- add: /{{ .ModulePath }}/modules/{{ .ModulePriority }}-{{ .ModuleName }}/images/{{ .ImageName }}/src
  to: /src/logpipe
---
image: {{ .ModuleName }}/{{ .ImageName }}-{{ $image_version }}
from: scratch
import:
- from: {{ .ModuleName }}/{{ .ImageName }}-artifact-{{ $image_version }}
  add: /
  to: /
  includePaths:
  - logpipe
  - install
  - uninstall
  before: setup
imageSpec:
  config:
    labels: {"distro": "all", "version": "all", "logpipe": "{{ $version }}" }
    clearUser: true
    clearWorkingDir: true
    clearCmd: true
    clearEntrypoint: true
    removeEnv: ["/.*/"]
---
image: {{ .ModuleName }}/{{ .ImageName }}-artifact-{{ $image_version }}
final: false
from: {{ index .Images "builder/distroless" }}
mount:
{{ include "mount points for golang builds" . }}
import:
- from: {{ .ModuleName }}/{{ .ImageName }}-src-artifact-{{ $image_version }}
  add: /src
  to: /src
  before: install
secrets:
- id: GOPROXY
  value: {{ .GOPROXY }}
packages:
- type: os-pm
  spec: [golang, git, coreutils]
- type: go-mod
  workdir: /src/logpipe
  env:
    GOPROXY: "%secret:GOPROXY%"
shell:
  setup:
  - cd /src/logpipe
  - GOPROXY=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o /logpipe logpipe.go
  - mv /src/scripts/* /
