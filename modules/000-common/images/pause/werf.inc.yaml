---
image: {{ $.ModuleName }}/{{ $.ImageName }}
from: {{ index $.Images "base/distroless" }}
import:
- from: common/distroless-artifact
  add: /relocate
  to: /
  before: setup
- from: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
  add: /pause-linux-amd64
  to: /pause
  before: setup
imageSpec:
  config:
    user: "64535:64535"
    entrypoint: ["/pause"]
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
from: common/src-artifact
final: false
git:
- add: /{{ $.ModulePath }}modules/{{ .ModulePriority }}-{{ $.ModuleName }}/images/{{ $.ImageName }}/src
  to: /src
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-artifact
from: {{ index $.Images "builder/distroless" }}
final: false
packages:
- type: os-pm
  spec: [gnu-gcc, binutils, gnu-glibc-devel, libgcc-static, bash, coreutils]
import:
- from: {{ $.ModuleName }}/{{ $.ImageName }}-src-artifact
  add: /src
  to: /src
  before: install
shell:
  install:
  - cd /src
  - gcc -Os -Wall -Werror -static -o /pause-linux-amd64 pause.c
  - strip /pause-linux-amd64
