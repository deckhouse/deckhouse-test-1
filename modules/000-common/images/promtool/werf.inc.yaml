{{- $prometheusVersion := "v2.55.1" }}
---
image: {{ .ModuleName }}/{{ .ImageName }}-src-artifact
from: common/src-artifact
final: false
git:
- url: {{ .SOURCE_REPO }}/prometheus/prometheus.git
  tag: {{ $prometheusVersion }}
  add: /
  to: /src/prometheus
- add: /{{ .ModulePath }}modules/{{ .ModulePriority }}-{{ .ModuleName }}/images/{{ .ImageName }}/patches
  to: /patches
  includePaths:
  - '**/*.patch'
  stageDependencies:
    install:
    - '**/*.patch'
shell:
  install:
  - cd /src/prometheus
  - git apply /patches/*.patch --verbose
---
image: {{ .ModuleName }}/{{ .ImageName }}-artifact
from: {{ index $.Images "builder/distroless" }}
final: false
import:
- from: {{ .ModuleName }}/{{ .ImageName }}-src-artifact
  add: /src/prometheus
  to: /src
  before: install
- from: common/promu-artifact
  add: /src/promu
  to: /usr/bin/promu
  before: install
mount:
{{ include "mount points for golang builds" . }}
secrets:
- id: GOPROXY
  value: {{ .GOPROXY }}
packages:
- type: os-pm
  spec: [golang==1.25.14, git, bash, coreutils, sed]
- type: go-mod
  workdir: /src
  env:
    GOPROXY: "%secret:GOPROXY%"
shell:
  install:
  - export GOPROXY=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64
  - cd /src
  - |
    while IFS=':' read -r key value; do
      if [[ -n $key && -n $value ]]; then
        key_clean=$(echo "$key" | tr -d '[:space:]' | tr '[:lower:]' '[:upper:]')
        value_clean=$(echo "$value" | sed 's/^ *//g')
        export "PROMETHEUS_${key_clean}=${value_clean}"
      fi
    done < <(/bin/promu info)
    export LDFLAGS="-X github.com/prometheus/common/version.Version=${PROMETHEUS_VERSION} \
      -X github.com/prometheus/common/version.Revision=${PROMETHEUS_REVISION} \
      -X github.com/prometheus/common/version.Branch=HEAD \
      -X github.com/prometheus/common/version.BuildUser=root@unknown \
      -extldflags '-static'"
  - go build -tags "netgo,stringlabels" -ldflags="${LDFLAGS}" -o /promtool ./cmd/promtool
  - chown 64535:64535 /promtool
  - chmod 0700 /promtool
