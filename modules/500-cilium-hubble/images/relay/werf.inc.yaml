# Based on https://github.com/cilium/cilium/blob/v1.17.4/images/hubble-relay/Dockerfile
# and https://github.com/cilium/cilium/blob/v1.17.4/images/hubble-relay/download-grpc-health-probe.sh
---
image: {{ $.ModuleName }}/{{ $.ImageName }}-build-artifact
from: {{ index $.Images "builder/distroless" }}
final: false
packages:
- type: os-pm
  spec: [golang==1.26.8, make, bash, binutils, git, coreutils, sed, grep, findutils]
- type: go-mod
  workdir: /src/cilium
  env:
    GOPROXY: "%secret:GOPROXY%"
import:
- from: cni-cilium/bin-cilium-src-artifact
  add: /src/cilium
  to: /src/cilium
  before: install
mount:
{{ include "mount points for golang builds" . }}
secrets:
- id: GOPROXY
  value: {{ .GOPROXY }}
shell:
  setup:
  - export GO_VERSION=$(go env GOVERSION) GOPROXY=off
  - export GOOS=linux GOARCH=amd64 CGO_ENABLED=0
  #
  - cd /src/cilium
  - go mod vendor
  - make DESTDIR=/out/linux/amd64 build-container-hubble-relay install-container-binary-hubble-relay
  - chown 64535:64535 /out/linux/amd64/usr/bin/hubble-relay
  - chmod 0700 /out/linux/amd64/usr/bin/hubble-relay
---
# Distroless Main Image
---
image: {{ $.ModuleName }}/{{ $.ImageName }}
from: {{ index $.Images "base/distroless" }}
import:
- from: common/distroless-artifact
  add: /relocate
  to: /
  before: install
- from: {{ $.ModuleName }}/{{ $.ImageName }}-build-artifact
  add: /out/linux/amd64/usr/bin/hubble-relay
  to: /usr/local/bin/hubble-relay
  before: install
- from: cni-cilium/gops-artifact
  add: /out/linux/amd64/bin/gops
  to: /usr/bin/gops
  before: install
{{- include "image mount points" . }}
imageSpec:
  config:
    user: "64535:64535"
    entrypoint: ["/usr/local/bin/hubble-relay", "serve"]
