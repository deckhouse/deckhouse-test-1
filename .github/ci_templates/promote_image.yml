{!{ define "promote_image_steps" }!}
{!{- $ctx := . -}!}
# <template: promote_image_steps>
{!{ tmpl.Exec "docker_config_isolation" | strings.Indent 4 }!}

    {!{- $secrets := slice -}!}
    {!{- $secrets = append ( slice "projects/data/6db2f1ee-9b6f-4f4f-8381-2fb43060478a/github/registry_host" "DECKHOUSE_REGISTRY_STAGE_HOST" "DECKHOUSE_REGISTRY_STAGE_HOST" ) $secrets -}!}
    {!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/stage-registry/writetoken" "login" "DECKHOUSE_REGISTRY_STAGE_USER" ) $secrets -}!}
    {!{- $secrets = append ( slice "projects/data/101ceaca-97cd-462f-aed5-070d9b9de175/stage-registry/writetoken" "password" "DECKHOUSE_REGISTRY_STAGE_PASSWORD" ) $secrets -}!}
    {!{ tmpl.Exec "import_secrets" $secrets | strings.Indent 4 }!}
{!{ tmpl.Exec "login_stage_registry_step" $ctx | strings.Indent 4 }!}
{!{ tmpl.Exec "werf_install_step" $ctx | strings.Indent 4 }!}

    - name: Promote images from stage to prod
      env:
        STAGE_REGISTRY_HOST: ${{ steps.secrets.outputs.DECKHOUSE_REGISTRY_STAGE_HOST }}
        DECKHOUSE_REGISTRY_HOST: ${{ secrets.DECKHOUSE_REGISTRY_HOST }}
      run: |
        set -euo pipefail

        # CE/EE/FE -> ce/ee/fe
        REGISTRY_SUFFIX=$(echo "${WERF_ENV}" | tr '[:upper:]' '[:lower:]')

        IMAGE_TAG=$(werf slugify --format docker-tag "${CI_COMMIT_TAG}")

        STAGE_PATH="${STAGE_REGISTRY_HOST}/deckhouse/${REGISTRY_SUFFIX}"
        PROD_PATH="${STAGE_REGISTRY_HOST}/deckhouse/promote-test/${REGISTRY_SUFFIX}"

        promote_with_att() {
          local SUBPATH="$1"
          local REPO_SRC REPO_DST SRC DST
          if [[ -z "${SUBPATH}" ]]; then
            REPO_SRC="${STAGE_PATH}"
            REPO_DST="${PROD_PATH}"
          else
            REPO_SRC="${STAGE_PATH}/${SUBPATH}"
            REPO_DST="${PROD_PATH}/${SUBPATH}"
          fi
          SRC="${REPO_SRC}:${IMAGE_TAG}"
          DST="${REPO_DST}:${IMAGE_TAG}"
          echo "⚓️ 💫 [$(date -u)] Promoting '${SRC}' -> '${DST}'"
          regctl image copy "${SRC}" "${DST}"
          local DIGEST
          DIGEST=$(regctl image digest "${SRC}" | sed 's/^sha256://')
          if regctl image manifest "${REPO_SRC}:sha256-${DIGEST}.att" >/dev/null 2>&1; then
            echo "⚓️ 💫 [$(date -u)] Copying attestation for '${REPO_SRC}'"
            regctl image copy "${REPO_SRC}:sha256-${DIGEST}.att" "${REPO_DST}:sha256-${DIGEST}.att"
          else
            echo "⚓️ 💫 [$(date -u)] Attestation for '${REPO_SRC}' not found, skipping..."
          fi
        }

        promote_with_att ""
        promote_with_att "install"
        promote_with_att "install-standalone"
        promote_with_att "release-channel"

        DIGESTS_JSON="$(mktemp)"
        trap 'rm -f "${DIGESTS_JSON}"' EXIT

        if regctl image get-file "${STAGE_PATH}/install:${IMAGE_TAG}" /deckhouse/candi/images_digests.json > "${DIGESTS_JSON}" 2>/dev/null; then
          echo "⚓️ 💫 [$(date -u)] Promoting component images from images_digests.json (by digest)"
          while IFS= read -r IMG_DIGEST; do
            [[ -z "${IMG_DIGEST}" ]] && continue
            STAGE_PATH="${STAGE_REGISTRY_HOST}/sys/deckhouse-oss"
            SRC_REF="${STAGE_PATH}@${IMG_DIGEST}"
            DST_REF="${PROD_PATH}@${IMG_DIGEST}"
            echo "⚓️ 💫 [$(date -u)] Promoting '${SRC_REF}' -> '${DST_REF}'"
            regctl image copy "${SRC_REF}" "${DST_REF}"
          done < <(jq -r '[.. | strings | select(test("^sha256:[a-f0-9]{64}$"))] | unique | .[]' "${DIGESTS_JSON}")
        else
          echo "⚠️ [$(date -u)] Could not read /deckhouse/candi/images_digests.json from '${STAGE_PATH}/install:${IMAGE_TAG}', skipping digest-based promotion"
        fi
# </template: promote_image_steps>
{!{ end }!}

{!{ define "promote_image" }!}
{!{- $ctx := . -}!}
# <template: promote_image>
detect_editions:
  name: Detect editions
  runs-on: "regular"
  outputs:
    PROMOTE_CE: ${{ steps.detect_editions.outputs.PROMOTE_CE }}
    PROMOTE_EE: ${{ steps.detect_editions.outputs.PROMOTE_EE }}
    PROMOTE_FE: ${{ steps.detect_editions.outputs.PROMOTE_FE }}
    PROMOTE_BE: ${{ steps.detect_editions.outputs.PROMOTE_BE }}
    PROMOTE_SE: ${{ steps.detect_editions.outputs.PROMOTE_SE }}
    PROMOTE_SE-plus: ${{ steps.detect_editions.outputs['PROMOTE_SE-plus'] }}
  steps:
    - name: Detect editions
      id: detect_editions
      env:
        EDITIONS: ${{ github.event.inputs.editions || '' }}
      run: |
        echo "Promote editions filter: '${EDITIONS}' (empty => all)"

        RESTRICTED=no

        for edition in CE EE FE BE SE SE-plus ; do
          if grep -i ",${edition}," <<<",${EDITIONS}," 2>/dev/null 1>&2 ; then
            echo "  - enable promote of ${edition} edition."
            echo "PROMOTE_${edition}=true" >> $GITHUB_OUTPUT
            RESTRICTED=yes
          fi
        done

        if [[ $RESTRICTED == "no" ]] ; then
          echo "No restrictions. Enable promote for all editions."
          for edition in CE EE FE BE SE SE-plus ; do
            echo "PROMOTE_${edition}=true" >> $GITHUB_OUTPUT
          done
        fi

promote_ce:
  name: Promote CE
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs.PROMOTE_CE == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: CE
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}

promote_ee:
  name: Promote EE
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs.PROMOTE_EE == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: EE
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}

promote_fe:
  name: Promote FE
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs.PROMOTE_FE == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: FE
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}

promote_be:
  name: Promote BE
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs.PROMOTE_BE == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: BE
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}

promote_se:
  name: Promote SE
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs.PROMOTE_SE == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: SE
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}

promote_se_plus:
  name: Promote SE-plus
  needs:
    - detect_editions
  if: ${{ needs.detect_editions.outputs['PROMOTE_SE-plus'] == 'true' }}
  runs-on: [self-hosted, stage]
  env:
    WERF_ENV: SE-plus
    CI_COMMIT_TAG: ${{ github.ref_name }}
  steps:
{!{ tmpl.Exec "promote_image_steps" $ctx | strings.Indent 4 }!}
# </template: promote_image>
{!{ end }!}
